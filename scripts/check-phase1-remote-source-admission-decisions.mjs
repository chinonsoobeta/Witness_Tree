import assert from "node:assert/strict";import{readFileSync}from"node:fs";
export function validateRemoteAdmissionDecisions(decisions,ledger){assert.equal(decisions.schemaVersion,1);assert.equal(decisions.status,"pending-owner-decisions");const remote=ledger.entries.filter(x=>x.evidenceState==="remote-verified-archived-profiled");assert.equal(decisions.decisions.length,remote.length);for(const d of decisions.decisions){const row=remote.find(x=>x.id===d.id);assert.ok(row);assert.ok(Object.entries(row.proof).filter(([k])=>k!=="productionAdmission").every(([,v])=>v));assert.equal(row.proof.productionAdmission,false);assert.equal(row.productionEligible,false);assert.equal(d.sourceEvidenceComplete,true);assert.ok(["pending","not-requestable"].includes(d.ownerAdmission));if(d.ownerAdmission==="not-requestable")assert.equal(d.profileIssueResolved,false);else assert.equal(d.profileIssueResolved,true);}return decisions}
if(import.meta.url===`file://${process.argv[1]}`){const read=p=>JSON.parse(readFileSync(new URL(`../data/${p}`,import.meta.url)));validateRemoteAdmissionDecisions(read("phase1-remote-source-admission-decisions.json"),read("phase1-production-source-ledger.json"));console.log("Remote source admission decisions remain pending and non-production.")}
