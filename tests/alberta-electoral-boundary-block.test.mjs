import assert from "node:assert/strict";import test from "node:test";import{validate}from"../scripts/check-alberta-electoral-boundary-block.mjs";import r from"../data/alberta-electoral-boundary-block.json"with{type:"json"};test("Alberta electoral remains rights-blocked",()=>{assert.doesNotThrow(()=>validate(r));assert.throws(()=>validate({...r,staged:true}));assert.throws(()=>validate({...r,productionEligible:true}))});
