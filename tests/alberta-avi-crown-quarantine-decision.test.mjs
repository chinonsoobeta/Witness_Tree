import assert from"node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";import{validate}from"../scripts/check-alberta-avi-crown-quarantine-decision.mjs";const x=p=>JSON.parse(readFileSync(new URL(p,import.meta.url)));test("rejects broader crown exclusion or a silent repair",()=>{const d=x("../data/alberta-avi-crown-quarantine-decision.json"),r=x("../data/transformation-runs/alberta-avi-geometry-repair-v1-2026-08-12.json");assert.doesNotThrow(()=>validate(d,r));const broad=structuredClone(d);broad.scope.crownObservationRecordsExcluded=1;assert.throws(()=>validate(broad,r));const repair=structuredClone(d);repair.scope.derivedGeometryWritten=true;assert.throws(()=>validate(repair,r))});
