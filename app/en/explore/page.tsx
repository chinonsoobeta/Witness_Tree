import type { Metadata } from "next"; import { SiteShell } from "@/components/site"; import { ExploreView } from "@/components/explore"; import { exploreFixtures, EXPLORE_MODES } from "@/lib/explore";
export const metadata: Metadata = { title: "Explore", alternates: { languages: { en: "/en/explore", fr: "/fr/explorer" } } };
export default async function Page({ searchParams }: { searchParams: Promise<{ mode?: string; view?: string }> }) { const q = await searchParams; const mode = EXPLORE_MODES.includes(q.mode as typeof EXPLORE_MODES[number]) ? q.mode as typeof EXPLORE_MODES[number] : "detected-change"; return <SiteShell locale="en"><main id="main" className="page-wrap"><h1>Explore forest change</h1><ExploreView events={exploreFixtures} locale="en" mode={mode} view={q.view === "table" ? "table" : "map"} /></main></SiteShell>; }
