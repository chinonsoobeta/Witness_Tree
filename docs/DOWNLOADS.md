# Example downloads

These are immutable example release and artifact contracts only. No CSV or GeoPackage binary has been retrieved or published. Each production artifact must carry the listed provenance, licence, checksum, and bilingual note. Example URLs are restricted to `https://example.local`; supplied bytes can be checked against the recorded SHA-256, but no binary is bundled here.
