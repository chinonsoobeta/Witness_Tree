# Phase 3 structural registry closure

This batch remains **example**, **unapproved**, **nonproduction**, and `productionEligible: false`. It introduces no real data, external action, deployment, or Phase 1/2 mutation.

## Coordinate and containment identity

All 32 synthetic locations use a deterministic URL-safe six-decimal coordinate permalink such as `lat-p48d000000-lon-n124d000000`, derived from their typed coordinate Figures. English and French routes are statically generated from the same coordinate identity and retain exact paired hreflang. A registry validator rejects coordinate drift, duplicate coordinate identities, and missing, duplicate, or unregistered containing geography IDs.

Each synthetic coordinate enumerates all eight applicable synthetic place types in its province. The resulting contract contains 256 checked containment links across the 32 locations. These are synthetic relationships only; they make no real geometry claim.

## Content, sources, citations, and downloads

Methods, data/source ledger, glossary, and corrections are rendered from `witness-tree/phase3-public-information/1`, one typed bilingual content/source registry. It requires every localized title, status, heading, paragraph, and link label, keeps all four surfaces bounded, and exposes the 32 registered illustrative source IDs. Every synthetic citation carries registered source IDs and resolves to the corresponding source-ledger anchor.

The former data-URI downloads are replaced by 32 deterministic CSV files and `witness-tree/phase3-example-download-manifest/1`. Every manifest entry fixes the file path, media type, byte length, SHA-256, place identity, and example/unapproved/nonproduction boundary. CI recomputes file bytes, checksum, and exact bounded content and rejects drift.

## Adapter and generated-record closure

The versioned synthetic Phase 2 adapter now preserves `lineageSha256`. It requires denominator reference year to equal the range start, nonnegative denominator and event hectares, Figure shares from 0 through 100 consistent with recomputed arithmetic, and nonblank English and French reasons for Unknown shares. Coordinated negative and bounded property-style cases cover these invariants.

The bilingual record/MDX validator accepts an injected record set. It rejects missing or duplicate records, untranslated strings, incomplete boundary front matter/body, mismatched bilingual keys, and inconsistent hreflang. Built no-JavaScript and local performance coverage includes paired place, coordinate-location, methods, source-ledger, glossary, and corrections route templates.

Browser focus traversal, assistive-technology behaviour, visual review, field performance, and measured LCP remain open; none is inferred from these deterministic gates.

## Incremental fixed-rubric self-assessment

The independent baseline after commit `0db0d04` is **55%**. This structural batch alone is conservatively assessed at **+4 points, 55% → 59%**, subject to independent audit: `+1` foundation/policy for the strengthened lineage and arithmetic adapter; `+2` public surfaces/generation for coordinate identity, full containment, registered citations/content and checksum-bound files; and `+1` bilingual/performance/accessibility/CI for injectable completeness plus broader built-route coverage. Earlier commits and their self-scores are not counted again.
