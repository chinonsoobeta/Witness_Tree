# Canonical Phase 1 production-source ledger

[`data/phase1-production-source-ledger.json`](../data/phase1-production-source-ledger.json) is the canonical reconciliation of the 31 production rows in the authoritative Phase 1 inventory, current as of 2026-08-27. It is deliberately separate from [`data/source-ledger.json`](../data/source-ledger.json), which remains an illustrative fixture and must never be mistaken for acquired data.

The ledger currently has two production-admitted rows: the two federal row identities bound to the same exact Elections Canada artifact and admission record. The remaining rows are not production eligible. The checker validates each admitted row through exactly one supported, checksum-bound production-admission record; it no longer assumes that every future admission is federal. The four current-wildfire rows have exact-version/COMPLIANCE primary archive evidence, but recovery, transformation, ingestion, release and production admission remain separate. The ledger fails closed if a row is omitted, raw credit is inflated, evidence references do not exist, an admission record is duplicated or unsupported, or production is inferred from archive/profile evidence alone.

Current evidence-state counts are: 14 remote-verified-archived-profiled, 1 local-verified-profiled (`cwfis-historical`), 1 partial-component, 13 access-blocked, and 2 production-admitted rows. This is 31 rows; none is unaddressed. The raw-evidence numerator is now **17.00/31**. The fixed 25-point baseline plus the 30-point raw-evidence component computes the bounded historical evidence-tracking score of **41.4516129%** (`25 + 30 × 17/31`, rounded to seven decimal places). The separate 45-point complete-ledger gate remains all-or-nothing and contributes zero until the complete core ledger passes. This tracker is not a phase-completion or release percentage. The formal Phase 1 exit remains **2/4 (50%)**, and the field-level audit currently proves **2/22 core rows complete and admitted**. Québec fourth-inventory has independent redacted exact-version/COMPLIANCE archive readback; recovery and every downstream decision remain separate. The PLVI exact legacy-manifest audit preserves two historical fieldCount:63 manifests without changing the canonical 60-attribute preparation or admitting production.

The exact NBAC ZIP acquired on 2026-08-27 under the current official Open Government Licence - Canada metadata is locally profiled only. It is 1,257,052,370 bytes with SHA-256 `c42740eb9d2fe3991a27344d0c33927705ec3e78c277efc5311b502439cb2165`; ZIP integrity passed, the profile records 52,610 polygons, and 49 ring self-intersections are quarantined. Archive, transformation, ingestion, release, publication, and production admission remain false. Owner authorization for acquisition and publication without waiting is recorded; the exact-key IAM delta is applied, but storage is not uploaded.

The one partial row and its exact missing components are reconciled in
[`PARTIAL_LEDGER_EVIDENCE_AUDIT.md`](PARTIAL_LEDGER_EVIDENCE_AUDIT.md). That
official-source audit adds no credit and accepts no publisher terms.

[`data/phase1-source-ledger-decision-readiness.json`](../data/phase1-source-ledger-decision-readiness.json) is the complementary audit. The NBAC `cwfis-historical` scope decision is recorded under current official terms and publication approval, while immutable archive, transformation, ingestion, release, and production evidence remain separate. Current-wildfire scope decisions likewise remain conditional on separately bound archive/recovery and downstream evidence. The Elections Canada 2025 SHP payload is one physical artifact shared by two required ledger rows, so it has one archival proof and two named row decisions; never duplicate uploads or immutable objects.
