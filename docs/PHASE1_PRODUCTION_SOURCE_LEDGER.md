# Canonical Phase 1 production-source ledger

[`data/phase1-production-source-ledger.json`](../data/phase1-production-source-ledger.json) is the canonical reconciliation of the 31 production rows in the authoritative Phase 1 inventory. It is deliberately separate from [`data/source-ledger.json`](../data/source-ledger.json), which remains an illustrative fixture and must never be mistaken for acquired data.

The ledger has exactly four production-eligible sources: `cwfis-current`, `bc-wildfire`, `ab-wildfire`, and `on-fire-disturbance`. Their explicit downstream approval activated only after the integrated six-payload immutable-readback gate passed. The other 27 rows remain fail-closed. The ledger still requires licence, attribution, retrieval/version, checksum, raw archive or official re-fetch path, profile, immutable archive, and an explicit production-admission record; it rejects admission inferred from archive or profile evidence alone.

Current evidence-state counts are: 11 remote-verified/archive/profiled, 5 local-verified/profiled, 2 partial components, and 13 access-blocked rows. This is 31 rows; none is unaddressed. The raw-evidence numerator is **15.25/31**. The fixed 25-point baseline plus the 30-point raw-evidence component computes a formal evidence-tracking score of **39.7580645%** (`25 + 30 × 15.25/31`, rounded to seven decimal places). The complete-ledger portion remains 0/31, therefore the separate 45% production-ledger component contributes **0 percentage points**. This score is not source-ledger admission, transformation, analysis, ingestion, public release, production admission, or production eligibility. The NRCan harvest and canopy-height rows now have redacted exact primary/recovery archive evidence, but their owner source-ledger decisions remain pending. The Québec fourth-inventory row is local verified/profiled only: its immutable archive, transformation, ingestion, admission, and eligibility fields remain false. Coverage-geometry completion does not change the production-ledger component. The Alberta PLVI row is remotely archived only: its record explicitly excludes owner source-ledger admission, transformation, analysis, ingestion, public release, production admission, and production eligibility. Every current access-blocked row has a primary-source, fail-closed resolution record in [`PHASE1_ACCESS_BLOCKER_RESOLUTION.md`](PHASE1_ACCESS_BLOCKER_RESOLUTION.md); the record supplies no source credit or admission.

The two partial rows and their exact missing components are reconciled in
[`PARTIAL_LEDGER_EVIDENCE_AUDIT.md`](PARTIAL_LEDGER_EVIDENCE_AUDIT.md). That
official-source audit adds no credit and accepts no publisher terms.

[`data/phase1-source-ledger-decision-readiness.json`](../data/phase1-source-ledger-decision-readiness.json) is the complementary audit. Four ordinary source-ledger decisions are recorded, seven rows still need immutable archive evidence before an ordinary decision, four current-wildfire scope decisions are recorded with the six-object primary readback gate complete but recovery/provenance and downstream decisions pending, one remotely archived row still needs a scope decision, and 15 remain externally blocked. The Elections Canada 2025 SHP payload is one physical artifact shared by two required ledger rows, so it has one archival proof and two named row decisions—never duplicate uploads or duplicate immutable objects.
