# Phase 2 real patch-vectorization preflight

The next owner-independent real-data batch remains blocked before national execution. No national patch output was created. A read-only profile opened four fixed 1024 × 1024 windows from the checksum-bound 2021–2022 loss raster. The windows contained 30,548 loss cells and 5,546 four-connected components within their truncated window edges; the measured profile took 0.122261 seconds. Those observations are useful for implementation tests, but they cannot establish national component counts because patches can cross window boundaries and the other 37 year-pairs were not inventoried.

The executable event contract now fixes four-neighbour connectivity, sorted unique cell lineage, one unsimplified cell polygon per loss cell, 0.09 hectares per cell, stable checksum-derived event IDs, the approved method identity, the source loss-raster SHA-256, and `released: false` / `productionEligible: false`. It rejects disconnected, duplicated, reordered, out-of-grid, wrong-year, or source-unbound patches.

The fail-closed capacity result is deliberate. Across 38 pairs the grid has 945,810,357,120 cell positions. If every position were changed, the decimal cell-index arrays alone would require 10,927,502,063,298 canonical bytes, before geometry or event fields, exceeding the approved 2 TiB cap. That is an absolute contract bound, not an estimate of actual loss. The bounded sample cannot replace an exact all-pair loss-cell inventory, a deterministic cross-window component stitcher with full-pair replay, or measured retained/scratch bytes and elapsed time. Until those three blockers close, the approved 8-vCPU, 16-GiB, 2-TiB, 96-hour national run must not start.

Run the source-backed profile gate with:

```sh
npm run check:phase2-real-patch-preflight
```

This increment adds no real patch/event output and therefore leaves the independently audited Phase 2 score at **51% (+0 percentage points)**. Boundary intersections, aggregates, samples/statistics, tiles, release, and production eligibility also remain open.
