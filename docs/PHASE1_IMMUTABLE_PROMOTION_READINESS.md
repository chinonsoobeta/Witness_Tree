# Phase 1 immutable-promotion readiness

This audit covers every one of the 5 current `local-verified-profiled` production rows and separately tracks the remotely verified canopy-height and current-wildfire raw archive evidence. It is a preparation map, not an admission decision.

| Physical group | Rows | Prepared state | Owner action still required |
| --- | --- | --- | --- |
| National two artifacts | `ntems-canopy-height`, `fed-2023-ridings`, `elections-canada-45th-files` | Canopy exact primary/recovery versions and COMPLIANCE retention are recorded in [`nrcan-canopy-height-remote-archive-evidence.json`](../data/nrcan-canopy-height-remote-archive-evidence.json). The federal ZIP remains one local physical artifact shared by two rows. | Do not revisit canopy. Record its separate owner source-ledger decision, and use only a separately approved federal-only path for the absent shared federal artifact. |
| Current wildfire snapshots | `cwfis-current`, `bc-wildfire`, `ab-wildfire`, `on-fire-disturbance` | Four raw and two derived payloads are locally checksum-bound. Their archive records omit concrete version identifiers, use placeholder checksum values, and therefore leave the six-object gate at 0/6 machine-verifiable (6/6 attested only). | Integrate concrete version and non-placeholder checksum bindings plus exact-version retention/readback proof before transformation, ingestion, release, or production decisions can activate. |
| Québec provincial current/original | `qc-current-ecoforest`, `qc-original-current-inventory` | Two exact large artifacts are prepared in [`qc-immutable-promotion-preparation.json`](../data/qc-immutable-promotion-preparation.json), with a dry-run-by-default sequential multipart runner. | Fresh approval for the two artifacts/four keys, exact role `WitnessTreeQcArchivePromotionUploader`, and its MFA-gated multipart/read-back procedure. Run `scripts/run-qc-approved-multipart-promotion.sh` with no arguments for its local dry run only. |
| Québec fourth inventory | `qc-fourth-inventory` | An exact 62-key preparation now binds all 56 sheet ZIPs, five evidence objects, and one collection manifest in [`qc-fourth-inventory-immutable-promotion-preparation.json`](../data/qc-fourth-inventory-immutable-promotion-preparation.json). Its dry run is `node scripts/qc-fourth-inventory-immutable-promotion.mjs`. | **Execution remains fail-closed.** Separate exact-artifact, irreversible COMPLIANCE-retention, least-privilege IAM, and MFA-session approvals are required. No AWS/IAM call, remote object, retention, read-back, transformation, admission, or eligibility is recorded. |

The full machine-readable reconciliation is [`data/phase1-immutable-promotion-readiness.json`](../data/phase1-immutable-promotion-readiness.json). It intentionally excludes PLVI because immutable archive evidence is already live, and it does not turn any archived or prepared artifact into a transformation, ingestion, release, source-ledger admission, production admission, or production-eligible source.
