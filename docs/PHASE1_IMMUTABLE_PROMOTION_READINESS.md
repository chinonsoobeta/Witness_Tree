# Phase 1 immutable-promotion readiness

This audit covers every one of the 11 current `local-verified-profiled` production rows. It is a preparation map, not an upload authorization or an admission decision. Every archive action remains separately owner-approved, MFA-gated, exact-key scoped, and subject to COMPLIANCE retention through `2033-08-12T00:00:00Z`, plus version, length, provider-checksum, and retention read-backs.

| Physical group | Rows | Prepared state | Owner action still required |
| --- | --- | --- | --- |
| National three artifacts | `ntems-forest-harvest`, `ntems-canopy-height`, `fed-2023-ridings`, `elections-canada-45th-files` | Three exact artifacts are already covered by [`phase1-local-profiled-promotion-preparation.json`](../data/phase1-local-profiled-promotion-preparation.json). The federal ZIP covers two rows once. | Do not duplicate the currently-running approved promotion. Await its prescribed read-backs before recording evidence. |
| Current wildfire snapshots | `cwfis-current`, `bc-wildfire`, `ab-wildfire`, `on-fire-disturbance` | Four exact paths, lengths, SHA-256 values, deterministic payload/sidecar keys, and dry-run output are in [`current-wildfire-immutable-promotion-preparation.json`](../data/current-wildfire-immutable-promotion-preparation.json). | Fresh approval for its four named artifacts and sidecars, the exact role `WitnessTreeCurrentWildfirePromotionUploader`, and its MFA-gated read-back procedure. Validate locally with `npm run dry-run:current-wildfire-immutable-promotion`. |
| Québec provincial current/original | `qc-current-ecoforest`, `qc-original-current-inventory` | Two exact large artifacts are prepared in [`qc-immutable-promotion-preparation.json`](../data/qc-immutable-promotion-preparation.json), with a dry-run-by-default sequential multipart runner. | Fresh approval for the two artifacts/four keys, exact role `WitnessTreeQcArchivePromotionUploader`, and its MFA-gated multipart/read-back procedure. Run `scripts/run-qc-approved-multipart-promotion.sh` with no arguments for its local dry run only. |
| Québec fourth inventory | `qc-fourth-inventory` | **Fail closed.** The local evidence proves the complete 56-sheet product only as an aggregate: 16,177,306,782 bytes and archive-set SHA-256 `394f05f984b164b7524e77b00fc73246a791d6c4112f7cc080c43fb3d8a2c0e0`. | First record every individual sheet’s exact local path, byte length, SHA-256, append-only key, and deterministic sidecar. Until then, no uploader role, approval wording, or owner command is safe to create. |

The full machine-readable reconciliation is [`data/phase1-immutable-promotion-readiness.json`](../data/phase1-immutable-promotion-readiness.json). It intentionally excludes PLVI because immutable archive evidence is already live, and it does not turn any archived or prepared artifact into a transformation, ingestion, release, source-ledger admission, production admission, or production-eligible source.
