# Phase 1 immutable-promotion readiness

This audit covers every one of the 6 current `local-verified-profiled` production rows and separately tracks the four current-wildfire rows whose raw archive evidence is now remote-verified while their two required derived objects remain unverified. It is a preparation map, not an upload authorization or an admission decision. Every archive action remains separately owner-approved, MFA-gated, exact-key scoped, and subject to COMPLIANCE retention through `2033-08-12T00:00:00Z`, plus version, length, provider-checksum, and retention read-backs.

| Physical group | Rows | Prepared state | Owner action still required |
| --- | --- | --- | --- |
| National two artifacts | `ntems-canopy-height`, `fed-2023-ridings`, `elections-canada-45th-files` | Two exact remaining local artifacts are covered by [`phase1-local-profiled-promotion-preparation.json`](../data/phase1-local-profiled-promotion-preparation.json). The federal ZIP covers two rows once. Harvest is excluded because its exact redacted primary/recovery evidence is recorded in [`nrcan-harvest-remote-archive-evidence.json`](../data/nrcan-harvest-remote-archive-evidence.json). The latest redacted operational record separately reports the canopy MPU complete and both primary/recovery payload and sidecar heads present with exact bytes and `FULL_OBJECT` checksums. | Canopy still needs exact-version readback and COMPLIANCE-retention evidence; the approved role currently lacks `s3:GetObjectVersion`, so no immutable archive credit is claimed. Do not re-upload or re-complete the canopy artifact. After the blocked readbacks are resolved, the runner's explicit federal-only mode can handle the absent federal key without revisiting harvest or canopy. Neither owner command is authorized by this audit. |
| Current wildfire snapshots | `cwfis-current`, `bc-wildfire`, `ab-wildfire`, `on-fire-disturbance` | Four exact raw payloads and sidecars have matching redacted primary/recovery version, byte, FULL_OBJECT CRC64NVME and COMPLIANCE-retention evidence. The owner-approved six-object gate remains 4/6 because the checksum-bound BC and Ontario derived releases are not remotely verified. | Do not treat the four raw readbacks or the owner scope decision as production admission. Obtain and integrate exact immutable readbacks for the two derived releases under the existing MFA-gated, exact-key procedure; validate the local plan with `npm run dry-run:current-wildfire-immutable-promotion`. |
| Québec provincial current/original | `qc-current-ecoforest`, `qc-original-current-inventory` | Two exact large artifacts are prepared in [`qc-immutable-promotion-preparation.json`](../data/qc-immutable-promotion-preparation.json), with a dry-run-by-default sequential multipart runner. | Fresh approval for the two artifacts/four keys, exact role `WitnessTreeQcArchivePromotionUploader`, and its MFA-gated multipart/read-back procedure. Run `scripts/run-qc-approved-multipart-promotion.sh` with no arguments for its local dry run only. |
| Québec fourth inventory | `qc-fourth-inventory` | An exact 62-key preparation now binds all 56 sheet ZIPs, five evidence objects, and one collection manifest in [`qc-fourth-inventory-immutable-promotion-preparation.json`](../data/qc-fourth-inventory-immutable-promotion-preparation.json). Its dry run is `node scripts/qc-fourth-inventory-immutable-promotion.mjs`. | **Execution remains fail-closed.** Separate exact-artifact, irreversible COMPLIANCE-retention, least-privilege IAM, and MFA-session approvals are required. No AWS/IAM call, remote object, retention, read-back, transformation, admission, or eligibility is recorded. |

The full machine-readable reconciliation is [`data/phase1-immutable-promotion-readiness.json`](../data/phase1-immutable-promotion-readiness.json). It intentionally excludes PLVI because immutable archive evidence is already live, and it does not turn any archived or prepared artifact into a transformation, ingestion, release, source-ledger admission, production admission, or production-eligible source.
